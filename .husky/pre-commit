#!/usr/bin/env sh
cd "$(dirname -- "$0")/.." || exit 1

export NVM_DIR="$HOME/.nvm"
[ -s "$NVM_DIR/nvm.sh" ] && \. "$NVM_DIR/nvm.sh"

cd client && npx lint-staged && cd .. || exit 1
cd server && npx lint-staged && cd .. || exit 1